{
  "name": "recipe-library",
  "private": true,
  "version": "0.0.0",
  "description": "Personal recipe-capture + meal-planning tool. See ARCHITECTURE.md and docs/execution-plan.md.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Violatsai/recipe_library.git"
  },
  "workspaces": [
    "server",
    "extension",
    "web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "migrate": "npm run migrate --workspace server",
    "smoke": "npm run smoke --workspace server",
    "import-bookmarks": "npm run import-bookmarks --workspace server --",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
